{
    "version": 2,
    "builds": [
      {
        "src": "./src/index.html",
        "use": "@vercel/build-utils/ssr",
        "config": {
          "devCommand": "npm run serve:ssr --${PORT}",
          "prodCommand": "npm run serve:ssr --${PORT}"
        }
      }
    ]
  }